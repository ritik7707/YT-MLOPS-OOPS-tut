1. class and obj (example)
2. class has data/attributes  and func/methods
     > code walkthrough
     > attributes get called itself methods are manually

3. why python is call OOPS?
everything in python is object (data structure, data types etc)

4. Advantages of oops:
    > You can create your own datatypes
    > reusability
    > debuggind
    > easy to colab

5. End to end Project (chatbook class)
    > function and method
    > magic methhod or dunder method
    > self
    > you can create attribute outside class as well

    > encapsulation
    > getter and setter
    > static method

encapsulation is a method to hide the given attribute within the class
getter is a method to get the hiden attribute 
setter is a method to set the hiden attribute 


static method is also a method that is bound to the class and not the object the class.

6. Inheritance
  > Inheritance code example
  > why Inheritance is needed ? (code reuseability, parent has no access to child class)
  > What get inherited? - Constructor , Non private attributes , Non private methods
  > Constructor overloading and  method overloading
  >super keyword (can be used only inside class, can not access variables, used only inside child class)




Constructor overriding happens when a child class defines its own Constructor (__init__) that replaces 
(overrides) the Constructor of the parent class . it works using Inheritance
  
  > Types of inheritance
    1. Single inheritance
    2. Multilevel inheritance
    3. Hierarchical Inheritance
    4. Multiple Inheritance (Dimond Problem)
    5. Hybrid Inheritance